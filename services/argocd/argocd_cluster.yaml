apiVersion: cluster.argocd.crossplane.io/v1alpha1
kind: Cluster
metadata:
  name: blanketops-environments-min-argocd-cluster
spec:
  forProvider:
    config:
      tlsClientConfig:
        insecure: true
    name: blanketops-environments-argocd-cluster
    server: https://kubernetes.default.svc
  providerConfigRef:
    name: default
