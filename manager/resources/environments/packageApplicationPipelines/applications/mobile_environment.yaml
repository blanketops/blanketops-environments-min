apiVersion: kappctrl.k14s.io/v1alpha1
kind: App
metadata:
  name: mobile-environment
  namespace: dev
spec:
  serviceAccountName: web-mobile-service-account
  fetch:
  - git:
      url: git@github.com:blanketops/blanketops-cloud-native-manifests.git
      ref: origin/develop
      subPath: compositions/mobile
      # must see more to 
  template:
  - ytt: {}
  deploy:
  - kapp: {}
