apiVersion: triggers.tekton.dev/v1beta1
kind: Trigger
metadata:
  name: trigger-github-push
spec:
  triggers:
  - name: github-listener
    interceptors:
    - ref:
        name: "github"
        kind: ClusterInterceptor
        apiVersion: triggers.tekton.dev
      params:
      - name: "secretRef"
        value:
          secretName: tekton-triggers-push-secret
          secretKey: token
      - name: "eventTypes"
        value: [ "push" ]
  bindings:
  - ref: pipeline-binding
  template:
    ref: pipeline-template
