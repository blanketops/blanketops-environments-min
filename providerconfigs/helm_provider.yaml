apiVersion: helm.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: blanketops-helm
spec:
  credentials:
    secretRef:
      key: kubeconfig
      name: cluster-config
      namespace: crossplane-system
    source: Secret
